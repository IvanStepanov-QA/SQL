1. Что такое СУБД? Какие ты знаешь СУБД?  
> СУБД, или система управления базами данных, — это программное обеспечение, предназначенное для создания, управления и организации данных в базах данных. СУБД позволяет пользователям и приложениям эффективно хранить, изменять и извлекать данные. Основные функции СУБД включают управление транзакциями, обеспечение безопасности данных, интеграцию данных и защиту от потерь.  
> Типы СУБД:
> - Реляционная база данных называется так от английского «relation», то бишь, «отношение». Имеется ввиду математическое понятие, которое для удобства нередко заменяют термином «таблица». Если изобразить структуру организации хранения данных графически, то схема действительно будет напоминать таблицу с ячейками. (Часто используемая база данных на практике).  
> - Иерархическая база данных по своей сути напоминает файловую систему в компьютере. Такая же древовидная структура с «корнем» и разветвлениями, разными уровнями иерархии. Поэтому файловые системы нередко относят к данной разновидности баз.  
> - Объектно-ориентированные базы данных представляют хранимую в них информацию в виде... да, правильно, объектов! А ещё их методов, классов и атрибутов. У каждого такого объекта есть собственный уникальный идентификатор, так что управляющий софт, написанный на каком-нибудь популярном языке программирования, найдёт нужное безошибочно.

2. Что такое БД? Какие бываю БД?
> База данных (БД) — это организованная коллекция данных, которая позволяет эффективно хранить, управлять и извлекать информацию. Базы данных могут использоваться для различных целей, от хранения пользовательских данных в приложениях до управления большими массивами информации в корпоративных системах.
> Типы баз данных:  
> - Реляционные базы данных (РБД):  
Хранят данные в таблицах, где строки представляют записи, а столбцы — их атрибуты.  
Примеры: MySQL, PostgreSQL, SQLite, Microsoft SQL Server, Oracle Database.  
> - Нереляционные базы данных (NoSQL):  
Не используют таблицы с фиксированной структурой. Подходят для работы с большими объемами неструктурированных или полуструктурированных данных.  
Включают:  
Документо-ориентированные базы данных (например, MongoDB).  
Колонно-ориентированные базы данных (например, Apache Cassandra).  
Графовые базы данных (например, Neo4j).  
Базы данных с ключ-значение (например, Redis).  
> - Объектно-ориентированные базы данных:  
Хранят данные в виде объектов, как в объектно-ориентированном программировании.  
Примеры: db4o, ObjectDB.  
> - Гибридные базы данных:  
Сочетают в себе характеристики реляционных и нереляционных баз данных и могут работать с различными типами данных.

3. В каком виде хранятся данные в Реляционных БД?
> - Строки (или записи): Каждая строка представляет отдельный экземпляр данных, то есть конкретную запись. Например, в таблице «Пользователи» каждая строка может содержать информацию о конкретном пользователе (имя, адрес электронной почты, дата рождения и т. д.).  
> - Столбцы (или поля): Каждый столбец соответствует атрибуту данных. Например, в таблице «Пользователи» могут быть столбцы «Имя», «Фамилия», «Email», «Возраст». Каждый столбец имеет определённый тип данных, такой как текст, число, дата и т. д.  
> - Первичные и внешние ключи:  
> * Первичный ключ — это уникальный идентификатор для каждой записи в таблице, который не может повторяться. Например, идентификатор пользователя (UserID) может быть первичным ключом в таблице «Пользователи».  
> * Внешний ключ — это поле, которое ссылается на первичный ключ другой таблицы, устанавливая тем самым связь между таблицами. Это позволяет организовать отношения между данными в разных таблицах. Например, в таблице «Заказы» внешний ключ может ссылаться на первичный ключ в таблице «Пользователи», чтобы указать, кто сделал заказ.  

4. Что такое SQL?  
> SQL (Structured Query Language) — это язык структурированных запросов, предназначенный для управления и работы с реляционными базами данных. Он используется для выполнения различных операций с данными.  

5. Какая разница между MySQL и SQL?  
> SQL (Structured Query Language) — это язык, используемый для работы с реляционными базами данных, в то время как MySQL — это одна из многих систем управления базами данных (СУБД), которая использует SQL в качестве своего языка запросов.  

6. Что такое первичный ключ (Primary key)? Как использовать?  
> Первичный ключ (Primary Key) — это один или несколько столбцов в таблице реляционной базы данных, которые служат уникальным идентификатором каждой записи в этой таблице. Основные характеристики первичного ключа:  
> * Уникальность: Значения в столбце первичного ключа должны быть уникальными для каждой записи. Это гарантирует, что можно однозначно идентифицировать каждую запись.  
> * Отсутствие значений NULL: Столбцы, определенные как первичный ключ, не могут содержать значение NULL. Каждая запись должна иметь значение первичного ключа.  
> * Неизменность: Значения первичного ключа, как правило, не изменяются после их присвоения записи. Это обеспечивает стабильность идентификатора записи в течение всего времени ее существования.  
> Примеры использования  
> * В таблице Пользователи первичным ключом может быть UserID, который уникально идентифицирует каждого пользователя.  
> * В таблице Продукты первичным ключом может быть ProductID, который назначается каждому продукту.  
> Зачем нужен первичный ключ?  
> * Идентификация записей: Позволяет быстро находить, обновлять и удалять записи по уникальному идентификатору.  
> * Связывание таблиц: Используется в качестве ссылки при определении внешних ключей (Foreign Keys), которые обеспечивают связь между различными таблицами в базе данных.  

7. Что такое внешний ключ (Foreign key)? Как использовать?  
> Внешний ключ (Foreign Key) — это поле или набор полей в одной таблице, которые ссылаются на первичный ключ другой таблицы. Внешние ключи устанавливают связь между таблицами и помогают поддерживать целостность данных в реляционной базе данных.  
> Основные характеристики внешнего ключа:  
> * Ссылка на первичный ключ: Внешний ключ в одной таблице ссылается на первичный ключ в другой таблице. Это означает, что значения внешнего ключа должны соответствовать значению первичного ключа или быть NULL.  
> * Целостность данных: Использование внешних ключей помогает обеспечить целостность данных, так как база данных будет контролировать, чтобы не было записей с внешним ключом, указывающим на несуществующий идентификатор в другой таблице.  
> * Связи между таблицами: Внешние ключи позволяют устанавливать отношения между таблицами, такие как «один ко многим» или «многие к одному».  

8. Что такое реляции? Зачем нужны реляции?  
> Реляции в контексте реляционных баз данных представляют собой таблицы, которые используются для организации и хранения данных. Каждая таблица (или реляция) состоит из строк (записей) и столбцов (атрибутов).  
Основные характеристики реляций:  
> * Структура:  
Каждая таблица содержит название, уникальные строки и атрибуты, которые описывают данные.  
Каждая строка в таблице представляет собой отдельный экземпляр сущности, а каждый столбец — ее свойство.  
> * Уникальность и целостность:  
Каждая таблица должна иметь первичный ключ, который уникально идентифицирует каждую запись.  
Система управления базами данных (СУБД) обеспечивает целостность данных через ограничения, такие как уникальность и внешние ключи.  
> Зачем нужны реляции:  
> 1) Организация данных:  
Реляции представляют собой упорядоченную структуру для хранения данных, что облегчает их управление и доступ.
> 2) Связи между данными:  
Реляционные базы данных позволяют устанавливать связи между различными таблицами с помощью внешних ключей. Это позволяет моделировать реальный мир, где разные сущности взаимосвязаны.  
> 3) Управление сложностью:  
Выделение данных в отдельные таблицы (нормализация) помогает избежать избыточности и обеспечивает более простой доступ к данным, сохраняя их организованными.  
> 4) Гибкость запросов:  
С использованием языка SQL (Structured Query Language) можно легко выполнять сложные запросы, соединять таблицы и извлекать данные по различным критериям.
> 5) Обеспечение целостности:  
Реляции помогают сохранять целостность данных через ограничения, такие как ограничения уникальности и внешние ключи, предотвращая несанкционированные изменения и ошибки.
> 6) Моделирование данных:  
Реляционные модели позволяют создавать структуру, отражающую отношения между данными, что делает их более интуитивно понятными и легкими для работы.

9. Приведи пример реляции, как она вообще работает?  
> Рассмотрим пример с двумя таблицами: Студенты и Курсы.  
> * Студенты:  
StudentID (первичный ключ)  
Имя  
Возраст  
> * Курсы:  
CourseID (первичный ключ)  
Название курса  
StudentID (внешний ключ, ссылается на StudentID из таблицы Студенты)  

> В этом примере мы можем легко установить связь между студентами и курсами, на которые они записаны, а также выполнять запросы, чтобы, например, узнать, какие студенты посещают определенный курс.

10. Какие ты знаешь JOIN? Распиши подробно про каждый.  
> В SQL существуют несколько типов операторов JOIN, которые позволяют соединять таблицы и извлекать данные на основе отношений между ними. Основные виды JOIN включают:  
> * INNER JOIN  
> * LEFT JOIN (или LEFT OUTER JOIN)  
> * RIGHT JOIN (или RIGHT OUTER JOIN)  
> * FULL JOIN (или FULL OUTER JOIN)  
> * CROSS JOIN  

> Давайте подробно рассмотрим каждый из этих типов:  
> 1) INNER JOIN  
INNER JOIN (внутреннее соединение) возвращает строки, которые имеют совпадения в обеих таблицах. Если строки в одной таблице не имеют соответствующих строк в другой, они не будут включены в результат.  
> 2) LEFT JOIN (LEFT OUTER JOIN)  
LEFT JOIN возвращает все строки из левой таблицы и совпадающие строки из правой таблицы. Если в правой таблице нет соответствующей строки, результат будет содержать NULL для колонок правой таблицы.  
> 3) RIGHT JOIN (RIGHT OUTER JOIN)  
RIGHT JOIN функционирует аналогично LEFT JOIN, но возвращает все строки из правой таблицы и совпадающие строки из левой таблицы. Если в левой таблице нет совпадения, вывод будет NULL для колонок левой таблицы.  
> 4) FULL JOIN (FULL OUTER JOIN)  
FULL JOIN возвращает строки, когда есть совпадение в одной из таблиц. Если в одной таблице нет соответствующих строк, результат будет содержать NULL для недостающих значений.  
> 5) CROSS JOIN  
CROSS JOIN возвращает декартово произведение двух таблиц. Это означает, что каждая строка первой таблицы будет соединена с каждой строкой второй таблицы. Обычно используется редко, так как результат может быть очень большим.  

11. Что бы работали JOIN нужны ли реляции?  

12. Что такое Union? Как использовать?  
> UNION — это оператор в SQL, который позволяет объединять результаты двух или более SELECT-запросов в один набор результатов. Он используется, когда необходимо получить уникальные строки из нескольких таблиц или запросов, которые имеют одинаковую структуру (то есть одинаковое количество столбцов и совместимые типы данных).  
Основные характеристики UNION:  
> * Уникальные строки: По умолчанию UNION удаляет дубликаты из итогового результата. Если нужно сохранить дубликаты, используется оператор UNION ALL.  
> * Совместимость структур: Все SELECT-запросы, которые объединяются с помощью UNION, должны возвращать одинаковое количество столбцов и соответствующие типы данных (например, строковые и числовые).  
> Порядок выполнения: Запросы выполняются по порядку, в котором они указаны. Результаты будут объединены в том порядке, в каком они были в отдельных запросах.  
> Синтаксис UNION:

        SELECT column1, column2, ...
        FROM table1
        UNION
        SELECT column1, column2, ...
        FROM table2;

13. В чем разница между UNION и UNION ALL ?  
> #### UNION:
> * Возвращает только уникальные строки.
> * Убирает дубликаты.
> #### UNION ALL:
> * Возвращает все строки, включая дублирующиеся.
> * Не удаляет дубликаты.

14. Какие существуют типы связей в базе данных? Приведите примеры.  
> В реляционных базах данных существуют три основных типа связей между таблицами: один к одному (1:1), один ко многим (1:N) и многие ко многим (M:N). Давайте рассмотрим каждый тип подробнее с примерами.  
> 1) Один к одному (1:1)  
В этой связи каждой записи в первой таблице соответствует ровно одна запись во второй таблице и наоборот. Этот тип связи редко используется, но может быть полезен для разделения специфичных данных.  
        CREATE TABLE person (
          person_id INT PRIMARY KEY,
          name VARCHAR(100)
        );
        
        CREATE TABLE passport (
          passport_id INT PRIMARY KEY,
          person_id INT,
          FOREIGN KEY (person_id) REFERENCES person(person_id)
        );
> 2) Один ко многим (1:N)  
Это наиболее распространенный тип связи, при котором одной записи в первой таблице соответствует множество записей во второй таблице. Однако каждой записи во второй таблице может соответствовать только одна запись в первой таблице.  
        CREATE TABLE department (
          department_id INT PRIMARY KEY,
          department_name VARCHAR(100)
        );
        
        CREATE TABLE employees (
          employee_id INT PRIMARY KEY,
          first_name VARCHAR(50),
          last_name VARCHAR(50),
          department_id INT,
          FOREIGN KEY (department_id) REFERENCES department(department_id)
        );
> 3) Многие ко многим (M:N)  
В этой связи одной записи в первой таблице может соответствовать множество записей во второй таблице, и наоборот. Этот тип связи реализуется с помощью промежуточной таблицы, которая хранит связи между двумя другими таблицами.  
        CREATE TABLE students (
          student_id INT PRIMARY KEY,
          student_name VARCHAR(100)
        );
        
        CREATE TABLE courses (
          course_id INT PRIMARY KEY,
          course_name VARCHAR(100)
        );
        
        CREATE TABLE enrollments (
          student_id INT,
          course_id INT,
          PRIMARY KEY (student_id, course_id),
          FOREIGN KEY (student_id) REFERENCES students(student_id),
          FOREIGN KEY (course_id) REFERENCES courses(course_id)
        );

15. Что такое SELECT ? Как использовать?  
> SELECT — это ключевое слово в языке SQL (Structured Query Language), используемое для выборки данных из одной или нескольких таблиц базы данных. Оно позволяет извлекать необходимые данные для анализа или отображения.  
Основной синтаксис команды SELECT  
        SELECT column1, column2, ...  
        FROM table_name  
        WHERE condition;  
        SELECT column1, column2, ...: указывает, какие колонки вы хотите увидеть в результатах. Можно использовать *, чтобы выбрать все колонки.
        FROM table_name: указывает таблицу, из которой будут извлечены данные.
        WHERE condition: (необязательный) фильтр, который позволяет ограничить результаты только теми строками, которые соответствуют указанному условию.

16. Что такое подзапрос? Как использовать?  
> Подзапрос — это запрос, который вложен внутри другого SQL-запроса. Подзапросы используются для выполнения более сложных выборок данных, позволяя сделать запросы более гибкими и мощными. Они помогают получать значения, необходимые для основного запроса, и могут использоваться в различных частях SQL-запроса, таких как SELECT, WHERE, FROM и других.  
Пример использования
Предположим, у вас есть две таблицы: employees и departments. Вы хотите получить имена всех сотрудников, работающих в департаменте, находящемся в определённом месте.
        SELECT first_name  
        FROM employees  
        WHERE department_id = (SELECT department_id FROM departments WHERE location_id = 1700);  
        Этот запрос найдет идентификатор департамента, находящегося по нужному адресу, а затем вернет имена сотрудников, работающих в этом департаменте.

17. Что такое CREATE TABLE? Как использовать?  
> CREATE TABLE — это команда в SQL, которая используется для создания новой таблицы в базе данных. С помощью этой команды вы можете определить структуру таблицы, указав названия колонок и их типы данных, а также задать другие характеристики, такие как ограничения (например, уникальность или обязательность значений).  

> Основной синтаксис команды CREATE TABLE  
        CREATE TABLE table_name (
            column1 data_type [constraints],
            column2 data_type [constraints],
            ...
        );  
table_name: название создаваемой таблицы.  
column1, column2, ...: названия колонок, которые будут в таблице.  
data_type: тип данных для каждой колонки (например, INT, VARCHAR, DATE, и т.д.).  
constraints: (необязательные) ограничения, которые можно применять к колонкам (например, PRIMARY KEY, NOT NULL, UNIQUE).  

18. Какие ты можешь использовать Числовые типы данных при создании таблицы?  
> При создании таблицы в SQL можно использовать различные числовые типы данных, которые зависят от конкретной базы данных (СУБД). Вот некоторые из наиболее распространенных числовых типов данных, которые поддерживаются в большинстве СУБД:  
> 1. Целочисленные типы (Integer Types)  
> * INT (или INTEGER): стандартный целочисленный тип. Обычно занимает 4 байта и хранит значения от -2,147,483,648 до 2,147,483,647.  
> * SMALLINT: меньший целочисленный тип, который занимает 2 байта и хранит значения от -32,768 до 32,767.  
> * TINYINT: еще меньший тип, занимающий 1 байт, и позволяющий хранить значения от 0 до 255 (или от -128 до 127, если используется знаковый тип).  
> * BIGINT: больший целочисленный тип, занимающий 8 байт и позволяющий хранить значения от -9,223,372,036,854,775,808 до 9,223,372,036,854,775,807.  
> 2. Числовые типы с фиксированной точностью (Fixed-Point Types)  
> DECIMAL(p, s) (или NUMERIC(p, s)): этот тип позволяет хранить числа с фиксированной точностью. p — общее количество цифр, а s — количество цифр после запятой. Например, DECIMAL(10, 2) может хранить числа до 10 цифр, из которых 2 — после запятой.  
> 3. Числовые типы с плавающей точкой (Floating-Point Types)  
> * FLOAT: тип, который позволяет хранить числа с плавающей точкой. Обычно используется для хранения больших или дробных значений. Количество байт, которое он занимает, зависит от конкретной реализации, но часто это 4 байта.  
> * DOUBLE: аналогично типу FLOAT, но с двойной точностью. Обычно занимает 8 байт и позволяет хранить более точные значения.

19. Какие ты можешь использовать Текстовые типы данных при создании таблицы?  
> При создании таблицы в SQL можно использовать различные текстовые типы данных, которые позволяют хранить строковые значения. Разные системы управления базами данных (СУБД) могут иметь свои особенности, но вот основные текстовые типы данных, которые обычно поддерживаются:  
> 1. Символьные типы
> * CHAR(n): фиксированная длина строки. Если строка короче n, она дополняется пробелами до указанной длины. Например, CHAR(10) всегда занимает 10 байт, даже если строка короче.  
> * VARCHAR(n): переменная длина строки. Позволяет хранить строки длиной до n символов. Занимает меньше места, чем CHAR, поскольку динамически использует только столько байт, сколько необходимо для хранения строки, плюс 1 или 2 байта для хранения длины.  
> 2. Текстовые типы
> * TEXT: этот тип данных используется для хранения больших объемов текстовой информации. Он обычно не имеет предела на количество хранимых символов (хотя конкретные ограничения могут зависеть от СУБД). Например, в MySQL TEXT может хранить до 65,535 символов.  
> * MEDIUMTEXT: аналогично TEXT, но позволяет хранить большие объемы данных — до 16,777,215 символов (в MySQL).
> * LONGTEXT: самый большой текстовый тип, который может хранить до 4,294,967,295 символов (в MySQL). 

> Примеры использования  
> Вот как можно использовать текстовые типы данных при создании таблицы:  
        CREATE TABLE users (
            user_id INT PRIMARY KEY,
            username VARCHAR(50) NOT NULL,  -- имя пользователя с максимальной длиной 50 символов
            password CHAR(60) NOT NULL,      -- фиксированная длина для хешированного пароля
            email VARCHAR(100),               -- email пользователя с максимальной длиной 100 символов
            bio TEXT                         -- текстовая справка о пользователе
        );

21. Какие ты можешь использовать типы данных Даты при создании таблицы?  
> При создании таблицы в SQL можно использовать различные типы данных для хранения информации о дате и времени. Конкретные типы могут немного различаться в зависимости от системы управления базами данных (СУБД), но вот основные типы данных, связанные с датами, которые обычно поддерживаются:  
> 1. Дата  
DATE: используется для хранения даты (год, месяц, день) без учета времени. Обычно формат хранения — YYYY-MM-DD. Например, 2024-11-27.
> 2. Время
>TIME: используется для хранения времени (часы, минуты, секунды) без учета даты. Формат хранения — HH:MM:SS. Например, 14:30:00.  
> 3. Дата и время
> * DATETIME: комбинированный тип, который хранит и дату, и время. Формат хранения — YYYY-MM-DD HH:MM:SS. Например, 2024-11-27 14:30:00. Этот тип полезен, когда необходимо хранить полную информацию о времени события.  
> * TIMESTAMP: также комбинированный тип для хранения даты и времени, но он обычно автоматически обновляется при изменении записи. TIMESTAMP также часто используется для записи времени создания и изменения записей. Хранит время относительно UTC (с учетом временной зоны) и имеет диапазон от 1970-01-01 00:00:01 UTC до 2038-01-19 03:14:07 UTC.
> 4. Интервал времени
> INTERVAL: этот тип данных позволяет хранить различия в времени. Например, можно использовать для вычислений, связанных с датами (например, чтобы добавить или вычесть время). Однако этот тип обычно не поддерживается во всех СУБД по единообразию.

22. Чем NULL отличается от 0?  
> * NULL:  
> Представляет отсутствие значения или неизвестное значение. Это специальный маркер в базах данных, который указывает на то, что ячейка не содержит никакого значения. Например, это может означать, что информация не была введена или недоступна.
> * 0 (ноль):  
Это конкретное числовое значение. Ноль может использоваться в математических операциях и считается "значением", хотя и равным нулю.

23. Какие ограничения существуют при создании таблиц?  
> При создании таблиц в базах данных много внимания уделяется ограничениям (constraints), которые помогают обеспечивать целостность данных и корректность их хранения. Вот основные ограничения, которые можно применять:  
> 1. Ограничения уникальности
>PRIMARY KEY:  
Обозначает уникальный идентификатор каждой строки в таблице.  
Не допускает значение NULL и дублирующиеся значения.  
UNIQUE:  
Гарантирует, что все значения в указанной колонке или группе колонок уникальны, позволяя при этом одно значение NULL.  
> 2. Ограничения целостности  
FOREIGN KEY:  
Указывает на связь между записями в двух таблицах.  
Гарантирует, что значение в одной таблице существует в другой таблице, поддерживая referential integrity (целостность ссылок).  
> 3. Ограничения на ненулевые значения  
NOT NULL:  
Задает требование, что колонка не может содержать значение NULL.  
Используется для обязательных полей, где отсутствие значения недопустимо.  
> 4. Ограничения проверки  
CHECK:  
Позволяет задавать условия для значений в колонках.  
Например, CHECK (age > 0 AND age < 120) гарантирует, что возраст находится в допустимом диапазоне.  
> 5. Ограничения на значение по умолчанию  
DEFAULT:  
Устанавливает значение по умолчанию для колонки, если при вставке записи оно не указано.  
Например, DEFAULT 'Pending' задаст статус по умолчанию.  
Пример создания таблицы с ограничениями  
> CREATE TABLE employees (  
    emp_id INT PRIMARY KEY,                     -- Уникальный идентификатор  
    first_name VARCHAR(50) NOT NULL,           -- Обязательное поле  
    last_name VARCHAR(50) NOT NULL,            -- Обязательное поле  
    email VARCHAR(100) NOT NULL UNIQUE,         -- Уникальный email  
    birth_date DATE CHECK (birth_date < CURRENT_DATE), -- Проверка даты рождения  
    department_id INT,                          -- Идентификатор отдела  
    FOREIGN KEY (department_id) REFERENCES departments(department_id) -- Внешний ключ  
);  

24. Что такое INSERT INTO? Как использовать?  
> INSERT INTO — это команда SQL, используемая для добавления новых записей (строк) в таблицу базы данных. С помощью этой команды можно вставить данные в одну или несколько колонок таблицы, а также указать значения для всех или некоторых колонок.  
> Основной синтаксис  
Вставка данных во все колонки:  
        INSERT INTO table_name VALUES (value1, value2, value3, ...);  
Вставка данных в выбранные колонки:
        INSERT INTO table_name (column1, column2) VALUES (value1, value2);  

25. Что такое UPDATE? Как использовать?
> UPDATE — это команда SQL, используемая для изменения существующих записей (строк) в таблице базы данных. Эта команда позволяет обновлять одно или несколько полей в одной или нескольких записях.  
Основной синтаксис  
Синтаксис команды UPDATE выглядит следующим образом:  
    UPDATE table_name  
    SET column1 = value1, column2 = value2, ...  
    WHERE condition;  
    table_name: имя таблицы, в которой вы хотите обновить данные.
    SET: указывает, какие колонки обновлять и их новые значения.
    WHERE: задает условие для выбора строк, которые нужно обновить. Если не указать условие, обновятся все записи в таблице.

24. Что такое DELETE, TRUNCATE и DROP? В чем их отличие?
> 1) DELETE  
Описание: Команда DELETE используется для удаления строк из таблицы на основе указанного условия.  
Синтаксис:  
    DELETE FROM table_name WHERE condition;  
Характеристики:  
> * Можно использовать условие WHERE для определения, какие строки удалять. 
> * Если WHERE не указано, все строки в таблице будут удалены. Удаление с помощью DELETE является транзакционным, что позволяет восстановить данные при необходимости (если ваша база данных поддерживает транзакции). 
> * Вызывается триггер при удалении строк, если он установлен.  
> 2) TRUNCATE  
Описание: Команда TRUNCATE используется для быстрого удаления всех строк из таблицы, но структура таблицы и ее определенные атрибуты (такие как схему) остаются нетронутыми.  
Синтаксис:
    TRUNCATE TABLE table_name;  
Характеристики:
> * Удаляет все строки в таблице, и это действие не может быть отменено.
> * Очень быстрое, так как не ведет журнал изменений для каждой строки, как это делает DELETE.
> * Не поддерживает условие WHERE.
> * Очистка таблицы с помощью TRUNCATE не вызывает триггеры.
> * Обычно не может быть использовано, если таблица имеет внешние ключи, ссылающиеся на неё. 
> 3) DROP  
Описание: Команда DROP используется для удаления всей таблицы или базы данных вместе с её структурой и всеми данными.  
Синтаксис:  
    DROP TABLE table_name;  
Характеристики:  
> * Удаляет таблицу полностью, включая её структуру, индексы, триггеры и все данные. После выполнения команды таблица больше не существует.
> * Это действие также не может быть отменено.
> * Не может быть выполнено, если на таблицу есть внешние ключи (в других таблицах), связывающиеся с ней (если установлены ограничения внешнего ключа).  

25. Что такое ALTER TABLE? Как использовать?  
> ALTER TABLE — это команда SQL, используемая для изменения структуры существующей таблицы в базе данных. С помощью этой команды можно добавлять, изменять или удалять столбцы, а также изменять ограничения (constraints) таблицы.  
Основной синтаксис  
Команда ALTER TABLE может использоваться в нескольких формах в зависимости от того, что именно вы хотите изменить:
> * Добавление нового столбца:
    ALTER TABLE table_name
    ADD column_name data_type [constraint];
> * Изменение существующего столбца:
    ALTER TABLE table_name
    MODIFY column_name new_data_type [constraint];
> * Удаление столбца:
    ALTER TABLE table_name
    DROP COLUMN column_name;
> * Изменение имени таблицы:
    ALTER TABLE old_table_name
    RENAME TO new_table_name;
> * Добавление, изменение или удаление ограничений:
    ALTER TABLE table_name
    ADD CONSTRAINT constraint_name constraint_type;

26. Какие знаешь агрегатные функции? Как использовать?  
> Агрегатные функции в SQL используются для выполнения вычислений над множеством значений и возвращают одно значение. Эти функции особенно полезны для анализа данных, создания отчетов и обобщения информации в базе данных.  
> * COUNT()
Описание: Возвращает количество строк (или количество ненулевых значений в указанном столбце).
> *  SUM()
Описание: Возвращает сумму значений в указанном столбце (числовом типе).
> *  AVG()
Описание: Возвращает среднее значение для числовых данных в указанном столбце.
> *  MAX()
Описание: Возвращает максимальное значение в указанном столбце.
> *  MIN()
Описание: Возвращает минимальное значение в указанном столбце.  

27. В чем разница между операторами GROUP BY и DISTINCT?  
> * DISTINCT
Описание: Оператор DISTINCT используется для выбора уникальных (различных) значений из одного или нескольких столбцов. Он удаляет дубликаты из результирующего набора.  
Синтаксис:  
    SELECT DISTINCT column1, column2, ...  
    FROM table_name;  
> * GROUP BY  
Описание: Оператор GROUP BY используется для объединения строк, которые имеют одинаковые значения в указанных столбцах, и позволяет выполнять агрегатные функции (например, COUNT, SUM, AVG) на этих группах.
Синтаксис:  
    SELECT column1, aggregate_function(column2)  
    FROM table_name  
    GROUP BY column1;

> DISTINCT и GROUP BY служат разным целям: DISTINCT фильтрует дубликаты, в то время как GROUP BY позволяет агрегировать данные по определенным полям. Выбор между ними зависит от того, какую задачу необходимо решить — получить уникальные значения или выполнить агрегатные вычисления по группам. Если у вас есть дополнительные вопросы или хотите рассмотреть другие примеры, дайте знать!  

28. В чем разница между операторами WHERE и HAVING?  
> * WHERE  
Оператор WHERE используется для фильтрации строк на этапе извлечения данных из таблицы. Он применим к отдельным строкам и позволяет отсеивать данные до выполнения любых агрегатных функций.  
> * HAVING  
Оператор HAVING используется для фильтрации групп данных после выполнения агрегатных функций. Он применяется в сочетании с оператором GROUP BY.  

29. Что такое Order by? Особенности работы Order by?  
> ORDER BY — это оператор SQL, используемый для сортировки результатов выборки по одному или нескольким столбцам. Он позволяет управлять порядком, в котором строки возвращаются в результате выполнения запроса, и это может быть по возрастанию (ASC) или по убыванию (DESC).  
Основной синтаксис  
    SELECT column1, column2, ...  
    FROM table_name  
    ORDER BY column1 [ASC|DESC], column2 [ASC|DESC], ...; 

30. Для чего используются операторы IN, BETWEEN, LIKE?  
> Операторы IN, BETWEEN и LIKE в SQL используются для фильтрации данных с определёнными условиями. Они облегчают формулирование запросов и позволяют проводить более гибкий отбор записей из таблиц.  
> * Оператор IN  
Описание: Оператор IN позволяет проверять, принадлежит ли значение заданному списку значений. Это удобно, когда нужно проверить несколько возможных значений для одного поля.  
Синтаксис:  
    SELECT column1, column2, ...  
    FROM table_name  
    WHERE column_name IN (value1, value2, value3, ...);  
> * Оператор BETWEEN  
Описание: Оператор BETWEEN используется для фильтрации значений в заданном диапазоне, включая границы. Он особенно полезен для работы с числовыми и датированными значениями.  
Синтаксис:  
    SELECT column1, column2, ...  
    FROM table_name  
    WHERE column_name BETWEEN value1 AND value2;  
> * Оператор LIKE  
Описание: Оператор LIKE используется для поиска строк, соответствующих определенному шаблону. Этот оператор полезен для работы с текстовыми данными, где требуется частичное совпадение.  
Синтаксис:  
SELECT column1, column2, ...  
FROM table_name  
WHERE column_name LIKE 'pattern';  

31. Какие условия выполнения операторов OR и AND?  
> Операторы OR и AND в SQL используются для комбинирования условий в выражениях WHERE, а также в других местах, где можно использовать логические условия, например, в HAVING. Они помогают задавать сложные фильтры для выборки данных из таблиц. Давайте рассмотрим, как они работают и их основные условия.  
> * Оператор AND  
 Оператор AND используется, чтобы объединить два или более логических условий. Все условия, соединенные оператором AND, должны быть истинными (true), чтобы итоговое выражение выполнялось.  
> * Оператор OR  
 Оператор OR позволяет выбрать строки, если хотя бы одно из условий истинно. Это значит, что для выполнения условия достаточно, чтобы одно из связанных условий было выполнено. 

32. Как работает оператор LIMIT?  
> Оператор LIMIT в SQL используется для ограничения количества строк, возвращаемых в результате выполнения запроса. Это особенно полезно, когда вы хотите получить только первые несколько записей из большого набора данных или когда необходима пагинация (разбиение на страницы) результата выборки.  
Синтаксис операторов LIMIT  
Применение оператора LIMIT может варьироваться в зависимости от используемой СУБД. В основном, он используется в запросах на выборку (SELECT).  
Основной синтаксис:  
    SELECT column1, column2, ...  
    FROM table_name  
    LIMIT number;  

33. Что такое нормализация и каковы ее преимущества?  
> Нормализация — это процесс организации данных в реляционных базах данных таким образом, чтобы минимизировать избыточность и зависимость данных. Это достигается путем структурирования таблиц и определения отношений между ними. Нормализация включает в себя несколько нормальных форм (нормальных уровней), каждая из которых имеет свои правила и требования.

Основные цели нормализации:
Устранение дублирующихся данных: Нормализация помогает избежать избыточности данных, что уменьшает количество дублирующихся записей в базе данных.
Избавление от аномалий: Упорядоченные структуры позволяют избежать аномалий при добавлении, обновлении и удалении данных (например, аномалии вставки, обновления и удаления).
Упрощение структуры данных: Нормализация помогает улучшить структуру базы данных, делая её более понятной и легкой для анализа.
Преимущества нормализации
Снижение избыточности и связанных затрат: Нормализация уменьшает дублирование данных, что помогает сократить расходы на хранение и управление.

Устранение аномалий: Она уменьшает вероятность возникновения аномалий при выполнении операций вставки, обновления и удаления, что делает операции с данными более надежными и безопасными.

Улучшение целостности данных: Нормализованные базы данных обычно имеют механизмы для более строгого обеспечения ссылочной целостности, что помогает сохранить достоверность данных.

Легкость в обслуживании и расширении: Структуры нормализованных данных проще изменять и расширять, так как относительно независимы друг от друга. Добавление новых атрибутов или таблиц менее вероятно нарушит существующие отношения.

Улучшение производительности запросов: Нормализация может потенциально улучшить производительность за счет уменьшения размера таблиц и упрощения операций присоединения (JOIN).

Недостатки нормализации
Стоит отметить, что нормализация также может иметь некоторые недостатки, такие как:

Сложность запросов: Нормализованные данные требуют более сложных запросов для получения информации, так как данные разбиты на несколько связанных таблиц.
Производительность операций: Избыточная нормализация может привести к снижению производительности из-за необходимости делать множество соединений между таблицами.

34. Какие формы нормализации существуют?  
> Основные нормальные формы
> 1) Первая нормальная форма (1NF): Таблица находится в первой нормальной форме, если:  
> * Все значения в столбцах атомарны (неделимы).
> * Каждый столбец содержит только одно значение и нет повторяющихся групп.
> 2) Вторая нормальная форма (2NF): Таблица находится во второй нормальной форме, если:
> * Она уже в 1NF.
> * Все неключевые атрибты полностью функционально зависят от первичного ключа (нет частичных зависимостей).
> 3) Третья нормальная форма (3NF): Таблица находится в третьей нормальной форме, если:
> * Она уже в 2NF.
> * Нет транзитивных зависимостей (никакие неключевые атрибты не зависят от других неключевых атрибтов).
> 4) Бойс–Кодд нормальная форма (BCNF): Это более строгая версия 3NF, где каждая зависимость X → Y подразумевает, что X является суперключом.  

35. Что такое денормализация?  
> Денормализация — это процесс преобразования структуры базы данных, при котором данные организуются не в нормализованной форме, а в более упрощенной, чтобы повысить производительность и упростить доступ к данным в определенных сценариях. Это может включать добавление избыточных данных, объединение таблиц или изменение нормализованных отношений для создания более прямых и быстрых запросов.  

> Основные характеристики денормализации:
> * Упрощение структуры: Денормализация может упростить структуру данных, путем уменьшения количества таблиц и связей между ними. Это может сделать схему базы данных более понятной, особенно для пользователей, не знакомых с нормализованными структурами.
> * Добавление избыточных данных: Денормализация часто включает в себя дублирование данных, чтобы уменьшить необходимость в сложных соединениях (JOIN) при выполнении запросов.
> * Увеличение производительности запросов: Упрощенная структура может привести к более быстрым запросам, так как меньше операций присоединения и больше данных хранится в одной таблице. Это особенно актуально для баз данных, которые часто читаются, но редко обновляются.

Из каких подмножеств состоит SQL?  


